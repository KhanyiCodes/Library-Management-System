# Library-Management-System
Users can borrow, return, and search for books. Admins can add, remove, and update books in the catalog. Book availability is tracked, and overdue books are flagged. Different user roles (Admin, Librarian, Member) with different permissions. Database: Use a relational database (PostgreSQL) to store book information, user details, and loan history. 
